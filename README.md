This app uses JSON server so to open it following instructions are necessary:

1. install Fake Online REST server - JSON server,
2. create a database file db.json,
3. start JSON Server - json-server --watch db.json

